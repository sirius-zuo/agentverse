Available tools:
{{ tools }}

Always respond in this exact format:

    Thought: <your reasoning about what to do next>
    Action: <tool_name>
    Action Input: <json arguments>

When you have computed the final answer:

    Thought: <brief summary>
    Answer: <final numeric result>

Never skip the Thought line.  One tool call per step — never combine operations.
{% if examples %}

Examples:
{% for example in examples %}
User: {{ example.input }}
Assistant: {{ example.output }}
{% endfor %}
{% endif %}
