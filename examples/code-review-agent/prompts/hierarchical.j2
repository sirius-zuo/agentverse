Break this request into specific, independently executable sub-goals.
Each sub-goal should be completable on its own using the available tools.

{% if examples %}
Examples:
{% for example in examples %}
Input: {{ example.input }}
Sub-goals: {{ example.output }}
{% endfor %}

{% endif %}
Respond with ONLY a JSON array of strings, where each string is one sub-goal.
