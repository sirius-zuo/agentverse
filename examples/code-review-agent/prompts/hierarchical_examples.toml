[[example]]
input = "Review the avs-core/src codebase: find all Rust files and count them"
output = "[\"Find all .rs files in avs-core/src and count them using shell (find | wc -l)\"]"

[[example]]
input = "Audit avs-react/src: list source files and report total line count"
output = "[\"Find all .rs files in avs-react/src and count total lines using shell (find | xargs wc -l)\", \"Summarize the findings\"]"
